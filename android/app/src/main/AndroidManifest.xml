<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Card Scanner"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!-- Hochkant festgenagelt (Spec B2, Abschlussreview I-2). Eine Drehung zerstoert die
             Activity, und Warteschlange, Rueckgaengig-Stapel und Staging des Einsortier-Modus
             haengen an `remember`. Sie waeren weg, OHNE Nachschreiben und ohne Verlust-Dialog:
             Karten steckten physisch in Faechern, digital existierte nichts davon, und der
             Nutzer erfuehre es nie. `configChanges` waere der feinere Weg, verlangt aber
             zusaetzlich, die Kameravorschau bei einer Drehung neu zu binden: beide
             ImageAnalysis-Bindungen setzen keine targetRotation und uebernehmen die
             Anzeigerotation zum Erzeugungszeitpunkt, der Scanner erkennt im Querformat sonst
             nichts mehr. Diese App ist eine Kamera-Begleitung fuers Kartensortieren; hochkant
             ist ihre Arbeitshaltung. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
